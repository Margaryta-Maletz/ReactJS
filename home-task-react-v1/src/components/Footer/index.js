import React from 'react';
import LogoIcon from '../LogoIcon';

const Footer = () => {
    return (
        <footer>
            <LogoIcon />
        </footer>
    )
}

export default Footer;
